# Validation

After making changes, run these commands to validate the codebase:

```bash
# TypeScript compilation check (strict mode)
npx tsc --noEmit

# Lint
npm run lint

# Build
npm run build
```

This is a Next.js 16 project with TypeScript (strict mode), ESLint 9, and Tailwind CSS 4.
